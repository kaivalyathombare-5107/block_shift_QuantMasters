'use client'
import { useState } from 'react'
import { ArrowDownRight, ArrowUpRight, Check, ChevronRight, Code2, ExternalLink, CircleDot, Linkedin, Mail, Menu, Minus, Sparkles, Star, X } from 'lucide-react'
import { ACHIEVEMENTS, BLOG_POSTS, DEVELOPER_PROFILE, GITHUB_STATS, PROJECTS, RESUME_METADATA, TECH_STACK } from '@/lib/data'
const nav = [['about','About'],['stack','Stack'],['projects','Projects'],['lab','Lab'],['achievements','Timeline'],['stats','Activity'],['connect','Connect']]
function Label({ n, children }: { n: string; children: React.ReactNode }) { return <div className="mb-5 font-mono text-[11px] uppercase tracking-[.22em] text-muted"><span className="text-accent">{n}</span><span className="mx-2 text-[#48505c]">/</span>{children}</div> }
function Button({ children, href, ghost=false }: { children: React.ReactNode; href?: string; ghost?: boolean }) { return <a href={href ?? '#connect'} className={`inline-flex items-center gap-3 rounded-full px-5 py-3 text-sm font-medium transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent ${ghost ? 'border border-border text-foreground hover:border-accent hover:text-accent' : 'bg-accent text-[#08101d] hover:bg-[#9bc2ff]'}`}>{children}<ArrowUpRight size={15}/></a> }
function Section({ id, children, className='' }: { id:string; children:React.ReactNode; className?:string }) { return <section id={id} data-section={id} className={`mx-auto max-w-6xl scroll-mt-24 px-5 py-24 md:px-8 ${className}`}>{children}</section> }
function Header() { const [open,setOpen]=useState(false); return <header className="fixed top-0 z-50 w-full border-b border-border/70 bg-[#08090c]/80 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5 md:px-8"><a href="#top" className="font-mono text-sm tracking-widest"><span className="text-accent">[</span> YN <span className="text-accent">]</span></a><nav className="hidden gap-5 lg:flex">{nav.map(([id,label])=><a key={id} href={`#${id}`} className="font-mono text-[11px] uppercase tracking-wider text-muted transition hover:text-foreground">{label}</a>)}</nav><a href="#connect" className="hidden rounded-full border border-accent/50 px-4 py-2 font-mono text-[11px] uppercase text-accent md:block">Let&apos;s talk</a><button aria-label="Toggle menu" onClick={()=>setOpen(!open)} className="rounded-md p-2 text-muted hover:text-foreground lg:hidden">{open?<X/>:<Menu/>}</button></div>{open&&<nav className="border-t border-border bg-surface px-5 py-8 lg:hidden">{nav.map(([id,label],i)=><a onClick={()=>setOpen(false)} key={id} href={`#${id}`} className="block border-b border-border py-4 font-display text-2xl">0{i+1} <span className="text-muted">{label}</span></a>)}</nav>}</header> }
function Hero() { return <section id="top" className="grid-fade relative flex min-h-[720px] items-center overflow-hidden px-5 pb-20 pt-32 md:px-8"><div className="pointer-events-none absolute right-[8%] top-32 h-[440px] w-[440px] rounded-full border border-accent/20 animate-float"><div className="absolute inset-8 rounded-full border border-accent/15"/><div className="absolute inset-20 rounded-full border border-accent/20"/><div className="absolute left-1/2 top-1/2 h-2 w-2 -translate-x-1/2 -translate-y-1/2 rounded-full bg-accent shadow-[0_0_36px_12px_rgba(110,168,255,.35)]"/></div><div className="relative mx-auto w-full max-w-6xl"><div className="max-w-3xl"><p className="mb-6 font-mono text-xs uppercase tracking-[.28em] text-accent">Computer Engineering · AI Systems</p><h1 className="font-display text-[clamp(3.8rem,10vw,8.5rem)] font-medium leading-[.86] tracking-[-.08em]">{DEVELOPER_PROFILE.name.split(' ').map((word,i)=><span key={word} className="block">{word}{i===0&&<span className="text-accent">.</span>}</span>)}</h1><p className="mt-8 font-mono text-sm text-muted"><span className="text-accent">&gt;_</span> {DEVELOPER_PROFILE.roles[0]}</p><p className="mt-5 max-w-lg text-base leading-7 text-muted">{DEVELOPER_PROFILE.bio[0]}</p><div className="mt-8 flex flex-wrap gap-3"><Button href="#projects">View my work</Button><Button href="#resume" ghost>Resume <span className="font-mono text-xs">PDF</span></Button></div></div><div className="mt-20 flex items-center gap-8 font-mono text-xs text-muted"><span className="flex items-center gap-2"><span className="h-2 w-2 animate-pulse rounded-full bg-[#73d69b]"/> Available for select work</span><span className="hidden md:block">Based in {DEVELOPER_PROFILE.location}</span></div></div><div className="absolute bottom-7 left-1/2 -translate-x-1/2 text-muted"><ArrowDownRight className="animate-bounce" size={18}/></div></section> }
function About() { return <Section id="about"><div className="grid gap-12 md:grid-cols-[.8fr_1.2fr]"><div><Label n="001">About</Label><h2 className="font-display text-4xl tracking-tight md:text-5xl">Making complex systems <span className="text-accent">feel clear.</span></h2></div><div className="space-y-5 text-lg leading-8 text-muted">{DEVELOPER_PROFILE.bio.slice(1).map(p=><p key={p}>{p}</p>)}<div className="grid grid-cols-3 gap-3 pt-6">{[['04','Projects'],['21','Tools'],['∞','Curiosity']].map(([n,l])=><div key={l} className="border-t border-border pt-3"><div className="font-display text-3xl text-foreground">{n}</div><div className="font-mono text-[10px] uppercase tracking-widest text-muted">{l}</div></div>)}</div></div></div></Section> }
function Stack() { const [cat,setCat]=useState('All'); const cats=['All','Languages','Frameworks','AI/ML','Cloud','Tools']; return <Section id="stack" className="border-t border-border"><Label n="002">Technology</Label><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><h2 className="font-display text-4xl tracking-tight md:text-5xl">The working set.</h2><div className="flex flex-wrap gap-2">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`rounded-full border px-3 py-2 font-mono text-[10px] uppercase tracking-wider transition ${cat===c?'border-accent bg-accent/10 text-accent':'border-border text-muted hover:text-foreground'}`}>{c}</button>)}</div></div><div className="mt-10 flex flex-wrap gap-2">{TECH_STACK.filter(t=>cat==='All'||t.category===cat).map(t=><div key={t.name} title={`${t.years} years experience`} className="group flex items-center gap-3 rounded-md border border-border bg-surface px-4 py-3 transition hover:-translate-y-1 hover:border-accent/60"><span className="font-mono text-[10px] text-accent">{t.icon}</span><span>{t.name}</span><span className="flex gap-0.5">{[1,2,3].map(n=><i key={n} className={`h-1.5 w-1.5 rounded-full ${n <= (t.proficiency==='Expert'?3:t.proficiency==='Proficient'?2:1)?'bg-accent':'bg-surface-3'}`}/>)}</span></div>)}</div></Section> }
function Projects() { return <Section id="projects"><Label n="003">Projects</Label><div className="mb-12 flex flex-col justify-between gap-4 md:flex-row md:items-end"><h2 className="font-display text-4xl tracking-tight md:text-5xl">Selected signals.</h2><p className="max-w-sm text-sm leading-6 text-muted">A few experiments and production systems from the lab.</p></div><div className="space-y-5">{PROJECTS.map((p,i)=><article key={p.slug} className={`group grid gap-6 border-t border-border py-8 md:grid-cols-[1fr_1.5fr] md:items-center ${i%2?'md:pl-16':''}`}><div className="flex aspect-[16/8] items-center justify-center overflow-hidden rounded-md border border-border bg-surface-2"><div className="relative h-28 w-28 rounded-full border border-accent/30"><div className="absolute inset-5 rounded-full border border-accent/50"/><div className="absolute left-1/2 top-1/2 h-1.5 w-1.5 -translate-x-1/2 -translate-y-1/2 bg-accent"/></div></div><div><div className="mb-4 flex items-center justify-between"><span className="font-mono text-xs text-muted">{p.date} · <span className="text-accent">{p.status}</span></span><ArrowUpRight className="text-muted transition group-hover:text-accent" size={20}/></div><h3 className="font-display text-3xl transition group-hover:text-accent">{p.title}</h3><p className="mt-3 max-w-xl leading-7 text-muted">{p.description}</p><div className="mt-5 flex flex-wrap gap-2">{p.tags.map(t=><span key={t} className="rounded border border-border px-2 py-1 font-mono text-[10px] text-muted">{t}</span>)}</div></div></article>)}</div></Section> }
function Lab() { return <Section id="lab" className="border-t border-border"><Label n="004">Lab / Writing</Label><div className="flex items-end justify-between"><h2 className="font-display text-4xl tracking-tight md:text-5xl">Notes from the edge.</h2><a href="/blog" className="hidden font-mono text-xs uppercase tracking-widest text-accent md:block">View all posts <ArrowUpRight className="inline" size={14}/></a></div><div className="mt-10 grid gap-4 md:grid-cols-3">{BLOG_POSTS.map((post,i)=><a href={`/blog/${post.slug}`} key={post.slug} className={`group border border-border bg-surface p-6 transition hover:-translate-y-1 hover:border-accent/60 ${i===0?'md:row-span-2 md:pt-10':''}`}><div className="mb-10 flex justify-between font-mono text-[10px] uppercase tracking-wider text-muted"><span className="text-accent">{post.tag}</span><span>{post.readTime}</span></div><h3 className="font-display text-2xl leading-tight group-hover:text-accent">{post.title}</h3><p className="mt-4 text-sm leading-6 text-muted">{post.excerpt}</p><div className="mt-8 border-t border-border pt-4 font-mono text-[10px] text-muted">{post.date}</div></a>)}</div></Section> }
function Timeline() { return <Section id="achievements"><Label n="005">Timeline</Label><h2 className="font-display text-4xl tracking-tight md:text-5xl">Proof of work.</h2><div className="relative mt-12 space-y-8 before:absolute before:bottom-4 before:left-[7px] before:top-4 before:w-px before:bg-border">{ACHIEVEMENTS.map(a=><div key={a.title} className="relative pl-10"><span className="absolute left-0 top-2 h-4 w-4 rounded-full border-4 border-background bg-accent"/><div className="flex flex-wrap items-baseline gap-x-4 gap-y-1"><span className="font-mono text-xs text-accent">{a.year}</span><h3 className="font-display text-xl">{a.title}</h3><span className="rounded border border-border px-2 py-1 font-mono text-[9px] uppercase text-muted">{a.type}</span></div><p className="mt-2 text-sm text-muted">{a.organization} — {a.description}</p></div>)}</div></Section> }
function Stats() { const cells=Array.from({length:91},(_,i)=>(i*7)%5); return <Section id="stats" className="border-t border-border"><Label n="006">Activity</Label><h2 className="font-display text-4xl tracking-tight md:text-5xl">Quietly consistent.</h2><div className="mt-10 grid grid-cols-2 gap-px overflow-hidden border border-border bg-border md:grid-cols-4">{[['Commits',GITHUB_STATS.commits,Code2],['Repositories',GITHUB_STATS.repos,CircleDot],['Pull requests',GITHUB_STATS.pullRequests,Check],['Stars earned',GITHUB_STATS.stars,Star]].map(([label,n,Icon])=><div key={label as string} className="bg-surface p-5"><Icon size={16} className="text-accent"/><div className="mt-5 font-display text-3xl">{n as number || '—'}</div><div className="font-mono text-[10px] uppercase tracking-widest text-muted">{label as string}</div></div>)}</div><div className="mt-8 border border-border bg-surface p-5"><div className="mb-4 flex justify-between font-mono text-[10px] uppercase tracking-widest text-muted"><span>Contribution matrix</span><span>Last 12 months</span></div><div className="grid grid-flow-col grid-rows-7 gap-1 overflow-hidden">{cells.map((v,i)=><i key={i} className={`h-3 w-3 rounded-sm ${v===0?'bg-surface-3':v===1?'bg-accent/30':v===2?'bg-accent/50':v===3?'bg-accent/75':'bg-accent'}`}/>)}</div></div></Section> }
function Resume() { return <Section id="resume" className="border-t border-border"><div className="grid gap-8 md:grid-cols-[.8fr_1.2fr] md:items-center"><div><Label n="007">Résumé</Label><h2 className="font-display text-4xl tracking-tight">A document of intent.</h2><p className="mt-5 text-muted">Experience, selected work, and the systems behind it.</p><Button href="#connect">Request résumé</Button></div><div className="flex min-h-56 items-center justify-center border border-dashed border-border bg-surface font-mono text-xs text-muted">{RESUME_METADATA.updatedAt||'Resume not yet connected'} <Minus className="mx-3 text-accent"/> PDF</div></div></Section> }
function Contact() { return <Section id="connect" className="border-t border-border"><div className="grid gap-12 md:grid-cols-2"><div><Label n="008">Connect</Label><h2 className="font-display text-5xl tracking-tight">Have a hard problem?<br/><span className="text-accent">Let&apos;s talk.</span></h2><p className="mt-6 max-w-md leading-7 text-muted">Open to thoughtful collaborations, product architecture, and ambitious experiments.</p><div className="mt-10 space-y-2">{DEVELOPER_PROFILE.socialLinks.map(s=><a key={s.platform} href={s.href} className="flex items-center justify-between border-b border-border py-4 text-sm transition hover:pl-2 hover:text-accent"><span className="flex items-center gap-3"><Mail size={16}/>{s.label}</span><ArrowUpRight size={16}/></a>)}</div></div><form className="space-y-4" onSubmit={e=>e.preventDefault()}><div className="grid gap-4 md:grid-cols-2"><input required aria-label="Name" placeholder="Your name" className="rounded-md border border-border bg-surface px-4 py-3 text-sm outline-none focus:border-accent"/><input required type="email" aria-label="Email" placeholder="Email address" className="rounded-md border border-border bg-surface px-4 py-3 text-sm outline-none focus:border-accent"/></div><input required aria-label="Subject" placeholder="What are we building?" className="w-full rounded-md border border-border bg-surface px-4 py-3 text-sm outline-none focus:border-accent"/><textarea required aria-label="Message" placeholder="A few words to start..." rows={6} className="w-full resize-none rounded-md border border-border bg-surface px-4 py-3 text-sm outline-none focus:border-accent"/><button className="inline-flex items-center gap-3 rounded-full bg-accent px-6 py-3 text-sm font-medium text-[#08101d] transition hover:bg-[#9bc2ff]">Send message <ArrowUpRight size={15}/></button></form></div></Section> }
export default function Page() { return <><Header/><main><Hero/><About/><Stack/><Projects/><Lab/><Timeline/><Stats/><Resume/><Contact/></main><footer className="border-t border-border px-5 py-8 md:px-8"><div className="mx-auto flex max-w-6xl flex-col justify-between gap-4 font-mono text-[10px] uppercase tracking-widest text-muted md:flex-row"><span><span className="text-accent">[</span> YN <span className="text-accent">]</span> — digital engineering lab</span><span>Built with intent · 2026</span></div></footer></> }
